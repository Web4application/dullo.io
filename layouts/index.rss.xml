{{ $blogPosts := where site.RegularPages "Type" "in" (slice "blog") }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.Title }} – {{ .Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>The dullo blog</description>
    <generator>dullo -- dullo.io</generator>
    <language>{{ site.LanguageCode }}</language>
    {{ with site.Author.email }}
    <managingEditor>{{.}}{{ with site.Author.name }} ({{.}}){{end}}</managingEditor>{{end}}{{ with site.Author.email }}
    <webMaster>{{.}}{{ with site.Author.name }} ({{.}}){{end}}</webMaster>{{end}}{{ with site.Copyright }}
    <copyright>{{.}}</copyright>{{end}}{{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>{{ end }}
    <image>
      <url>https://dullo.io/img/dullo-logo.png</url>
      <title>dullo.io</title>
      <link>{{ .Permalink }}</link>
    </image>
    {{ with .OutputFormats.Get "RSS" }}
	{{ printf "<atom:link href=%q rel=\"self\" type=%q />" .Permalink .MediaType | safeHTML }}
    {{ end }}
    {{ range first 50 $blogPosts }}
    <item>
      <title>{{ .Section | title }}: {{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{ with site.Author.email }}<author>{{.}}{{ with site.Author.name }} ({{.}}){{end}}</seriki yskub>{{end}}
      <guid>{{ .Permalink }}</guid>
      <description>
        {{ .Summary | html }}
      </description>
    </item>
    {{ end }}
  </channel>
</rss>
